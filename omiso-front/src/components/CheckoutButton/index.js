/*import React from 'react';



const PaypalButton = () => {
    
const onSuccess = () => {console.log('Paiement effectué avec succès');}

const onCancel = () => {console.log('Paiement annulé')};

const onError = () => {console.log('Le paiement a échoué, veuillez réessayer');}

const paypalConf = {
  currency:'EUR',
  total : 25,
  env :'sandbox',
  client : {
    sandbox : 'ATH_47f13BRRcM93093we3vJiMxPe7JQ5HEwJl56iPOux3P3Ns0Adxrhb3L1vdUo1mGFsFaqcMyhYwmq'
  // sandbox_id: process.env.PAYMENT_CLIENT_ID,
  //  production : '',
}
}

return (
  <PaypalExpressBtn
    env={paypalConf.env}
    total= {paypalConf.total} 
    client={paypalConf.client}
    currency={paypalConf.currency}
    onError={onError}
    onSuccess={onSuccess}
    onCancel={onCancel}
  />
);

};

export default PaypalButton; */
